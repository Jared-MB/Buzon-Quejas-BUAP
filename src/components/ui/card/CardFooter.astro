---
import { cn } from "@/lib/utils";

interface Props {
    className?: string;
}

const { className } = Astro.props;

const footerClass = cn(
    "bg-zinc-300/50 rounded-b px-6 py-5 flex flex-row items-center",
    className,
);
---

<footer class={footerClass}>
    <slot />
</footer>
